# =============================================================================
# Single-Server Inventory Example
# =============================================================================
# Copy + edit for your deployment:
#   cp inventories/_examples/single-server.example.yml inventories/<env>/hosts.yml
# =============================================================================

all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
    ansible_python_interpreter: /usr/bin/python3
    timezone: UTC

  hosts:
    web01:
      ansible_host: 203.0.113.10
      description: 'Web server'
