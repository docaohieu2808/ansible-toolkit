# =============================================================================
# Role: Users - Handlers
# =============================================================================

---
- name: Reload SSH
  ansible.builtin.systemd:
    name: sshd
    state: reloaded
